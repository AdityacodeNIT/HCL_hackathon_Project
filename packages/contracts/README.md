# Shared Contracts Package

API contracts, DTOs, validation schemas, and shared constants.

## Purpose

This package defines the boundary between frontend and backend:
- Request/response shapes
- Validation schemas
- Error codes
- Role constants
- Shared enums

## Usage

Both frontend and backend import from this package to ensure type safety and contract alignment.
